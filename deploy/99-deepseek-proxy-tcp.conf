# TCP keepalive tuning for long-lived client streams (EC2 / Linux).
# Apply: sudo cp ... /etc/sysctl.d/99-deepseek-proxy-tcp.conf && sudo sysctl --system

net.ipv4.tcp_keepalive_time = 60
net.ipv4.tcp_keepalive_intvl = 10
net.ipv4.tcp_keepalive_probes = 9
